<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aihoo.domain.sys.model.mapper.SysMenuMapper">

    <select id="listByUserId" resultType="com.aihoo.domain.sys.model.entity.SysMenu">
        select * from t_sys_menu where id in (
          select menu_id from t_sys_role_menu where role_id in (
            select role_id from t_sys_user_role where user_id = #{userId}
          )
      ) order by order_number desc
    </select>

    <select id="listByRoleIds" resultType="com.aihoo.domain.sys.model.entity.SysMenu">
        select * from t_sys_menu where id in (
        select id from t_sys_role_menu where role_id in
        <foreach collection="roleIds" separator="," item="roleId">
            #{roleId}
        </foreach>
        )
        )
    </select>

    <select id="listByRoleId" resultType="com.aihoo.domain.sys.model.entity.SysMenu">
        select * from t_sys_menu where id in (
          select menu_id from t_sys_role_menu where role_id = #{roleId}
        ) order by order_number desc
    </select>

</mapper>
