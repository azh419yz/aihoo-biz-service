<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aihoo.domain.patient.model.mapper.WechatUserMapper">

<update id="isWechatUser" parameterType="com.aihoo.domain.patient.model.entity.WechatUser">
    UPDATE t_wechat_user SET
    <if test="null!=state and !''.equals(state)">
        state=#{state}
    </if>
    <if test="null!=isLogin and !''.equals(isLogin)">
        ,is_login=#{isLogin}
    </if>
        ,start_time=date_format(#{startTime},'%Y-%c-%d %H:%i:%s')
        ,end_time=date_format(#{endTime},'%Y-%c-%d %H:%i:%s')
    where id=#{id}
</update>
</mapper>
